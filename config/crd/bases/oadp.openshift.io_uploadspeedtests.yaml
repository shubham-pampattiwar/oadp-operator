---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: uploadspeedtests.oadp.openshift.io
spec:
  group: oadp.openshift.io
  names:
    kind: UploadSpeedTest
    listKind: UploadSpeedTestList
    plural: uploadspeedtests
    singular: uploadspeedtest
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UploadSpeedTest is the Schema for the uploadspeedtests API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UploadSpeedTestSpec defines the desired state of UploadSpeedTest
            properties:
              backupLocation:
                description: BackupLocation defines relevant configuration of the
                  object storage/backup storage location to be tested
                properties:
                  bucket:
                    description: CloudStorageLocation defines BackupStorageLocation
                      using bucket referenced by CloudStorage CR.
                    properties:
                      backupSyncPeriod:
                        description: backupSyncPeriod defines how frequently to sync
                          backup API objects from object storage. A value of 0 disables
                          sync.
                        nullable: true
                        type: string
                      caCert:
                        description: CACert defines a CA bundle to use when verifying
                          TLS connections to the provider.
                        format: byte
                        type: string
                      cloudStorageRef:
                        description: |-
                          LocalObjectReference contains enough information to let you locate the
                          referenced object inside the same namespace.
                        properties:
                          name:
                            description: |-
                              Name of the referent.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              TODO: Add other useful fields. apiVersion, kind, uid?
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      config:
                        additionalProperties:
                          type: string
                        description: config is for provider-specific configuration
                          fields.
                        type: object
                      credential:
                        description: credential contains the credential information
                          intended to be used with this location
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            description: |-
                              Name of the referent.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              TODO: Add other useful fields. apiVersion, kind, uid?
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      default:
                        description: default indicates this location is the default
                          backup storage location.
                        type: boolean
                      prefix:
                        description: Prefix is the path inside a bucket to use for
                          Velero storage. Optional.
                        type: string
                    required:
                    - cloudStorageRef
                    type: object
                  name:
                    type: string
                  velero:
                    description: BackupStorageLocationSpec defines the desired state
                      of a Velero BackupStorageLocation
                    properties:
                      accessMode:
                        description: AccessMode defines the permissions for the backup
                          storage location.
                        enum:
                        - ReadOnly
                        - ReadWrite
                        type: string
                      backupSyncPeriod:
                        description: BackupSyncPeriod defines how frequently to sync
                          backup API objects from object storage. A value of 0 disables
                          sync.
                        nullable: true
                        type: string
                      config:
                        additionalProperties:
                          type: string
                        description: Config is for provider-specific configuration
                          fields.
                        type: object
                      credential:
                        description: Credential contains the credential information
                          intended to be used with this location
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            description: |-
                              Name of the referent.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              TODO: Add other useful fields. apiVersion, kind, uid?
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      default:
                        description: Default indicates this location is the default
                          backup storage location.
                        type: boolean
                      objectStorage:
                        description: ObjectStorageLocation specifies the settings
                          necessary to connect to a provider's object storage.
                        properties:
                          bucket:
                            description: Bucket is the bucket to use for object storage.
                            type: string
                          caCert:
                            description: CACert defines a CA bundle to use when verifying
                              TLS connections to the provider.
                            format: byte
                            type: string
                          prefix:
                            description: Prefix is the path inside a bucket to use
                              for Velero storage. Optional.
                            type: string
                        required:
                        - bucket
                        type: object
                      provider:
                        description: Provider is the provider of the backup storage.
                        type: string
                      validationFrequency:
                        description: ValidationFrequency defines how frequently to
                          validate the corresponding object storage. A value of 0
                          disables validation.
                        nullable: true
                        type: string
                    required:
                    - objectStorage
                    - provider
                    type: object
                type: object
              cloudProviderSecretRef:
                description: CloudProviderSecretRef is the reference to the secret
                  to be used for authentication with object storage
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
              uploadSpeedTestConfig:
                description: UploadSpeedTestConfig defines the parameters for testing
                  upload speed
                properties:
                  fileSize:
                    description: size of the file to be used for test
                    type: string
                  testTimeout:
                    description: timeout value for the upload test operation
                    type: string
                required:
                - fileSize
                - testTimeout
                type: object
            required:
            - backupLocation
            - cloudProviderSecretRef
            - uploadSpeedTestConfig
            type: object
          status:
            description: UploadSpeedTestStatus defines the observed state of UploadSpeedTest
            properties:
              errorMessage:
                description: Details of any error encountered
                type: string
              lastTested:
                description: Timestamp of the last upload speed test
                format: date-time
                type: string
              phase:
                description: Status of the test operation - Complete, Failed
                type: string
              speed:
                description: Upload Speed of the operation
                format: int64
                type: integer
            required:
            - errorMessage
            - lastTested
            - phase
            - speed
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
